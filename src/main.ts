import { entityOne } from "entitys";
import { _dirname } from 'use_libs'


// console.log("Hello Word")

console.log(`LIB DIRNAME >> ${_dirname} === ACABOU_LIB.`)

// console.log(`Usando Deno.cwd() esta voltando : ${Deno.cwd()} --- final.`)

console.log(entityOne())